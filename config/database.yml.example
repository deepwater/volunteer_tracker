defaults: &defaults
  adapter: postgresql
  encoding: utf8
  database: ggf_volunteer_tracker
  host: 127.0.0.1

development:
  <<: *defaults

staging:
  <<: *defaults
  database: volunteer_tracker_staging

production:
  <<: *defaults
  database: volunteer_tracker_production

test:
  <<: *defaults
  database: volunteer_tracker_test