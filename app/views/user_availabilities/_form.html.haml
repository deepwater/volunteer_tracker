= simple_form_for(UserAvailability.new, url: user_user_availabilities_path(user), remote: true) do |f|
  =f.hidden_field(:day_id, value: day.id)
  =f.submit(class: 'btn btn-danger', value: "Unavailable")