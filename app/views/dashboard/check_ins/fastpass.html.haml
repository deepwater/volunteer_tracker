.row
  .span12
    %h2
      Fastpass Check-In

    %p Scan the barcode when you are ready

    = form_for @check_in, url: dashboard_check_ins_path(fastpass: true), remote: true, html: { "data-type" => 'json' }  do |f|
      = f.text_field :user_schedule_id, autofocus: true
      = f.hidden_field :status, value: "1"
.row
  .span4
    %h3
      Checked-In
    %p A list of checked in users this FastPass session

    %table.table.table-bordered.table-striped#fast-pass-table
      %thead
        %tr
          %th User Name
          %th User Schedule ID
      %tbody