$("#promote-table tbody").empty().append("<%= j(render 'promote_items', users: @users)%>");
$("#pagination-promote-users").empty().append("<%= j(paginate @users, remote: true) %>")
$("#pagination-info-promote-users").empty().append("<%= j(page_entries_info @users) %>")