.row
	.span9
		%h2 
			= @department.name

- if notice
	.row
		.span9
			.alert.alert-block
				%p#notice= notice
.row
	.span9
		%p
			%b Name:
			= @department.name
		%p
			%b Budgeted Hours:
			= @department.budgeted_hours
		%p
			%b Manager:

			- if !@department.department_managers.nil?
				- @department.department_managers.each do |department_manager|
					= link_to department_manager.user.full_name,  admin_user_path(department_manager.user)
			- else
				= link_to(edit_admin_department_path(@department)) do
					%button.btn.btn-small.btn-success{:type=>"button"} 
						Assign Manager

		= link_to 'Edit', edit_admin_department_path(@department)
		\|
		= link_to 'Delete', admin_department_path(@department), method: :delete, data: { confirm: 'Are you sure?' }
		\|
		= link_to 'Back', admin_departments_path
