.page-header
  h1 = @charity.name

= link_to 'Back', admin_root_url(anchor: 'charities'), class: 'btn btn-default'
'
= link_to 'Edit', edit_admin_charity_path(@charity), class: 'btn btn-primary'
'
= link_to 'Delete', admin_charity_path(@charity), class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' }
