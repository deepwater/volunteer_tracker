.page-header
  h1 = @organisation.name

= link_to 'Back', admin_root_url(anchor: 'organisations'), class: 'btn btn-default'
'
= link_to 'Edit', edit_admin_organisation_path(@organisation), class: 'btn btn-primary'
'
= link_to 'Delete', admin_organisation_path(@organisation), class: 'btn btn-danger', method: :delete, data: { confirm: 'Are you sure?' }
