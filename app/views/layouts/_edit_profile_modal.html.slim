.modal.fade.edit-profile-modal
  .modal-dialog.modal-lg
    .modal-content
      .modal-header
        button.close aria-label="Close" data-dismiss="modal" type="button"
          span aria-hidden="true"  &times;
        h4.modal-title
      .modal-body
      .modal-footer
        button.btn.btn-default data-dismiss="modal" type="button"  Close
        button.btn.btn-primary.edit-profile-modal-save type="button"  Save changes


  /! /.modal-content
  /! /.modal-dialog
/! /.modal

javascript:
  $(".edit-profile-modal").on('hidden.bs.modal', function (e) {
    $title = $(this).find(".modal-title")
    $body = $(this).find(".modal-body")

    $title.html("");
    $body.html("");
  });

  $(".edit-profile-modal .edit-profile-modal-save").click(function(e){
    $(".edit-profile-modal form").submit();
  });
