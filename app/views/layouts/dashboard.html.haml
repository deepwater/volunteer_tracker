!!!
%html
	%head
	%title VolunteerTracker Dashboard

	= stylesheet_link_tag    "application", :media => "all"
	= javascript_include_tag "application"
	= csrf_meta_tags

	%body
		.container
      
			%noscript
				.row
					.alert.alert-block.span11
						%h4.alert-heading Warning!
						%p
							You need to have
							%a{:href => "http://en.wikipedia.org/wiki/JavaScript", :target => "_blank"} JavaScript
							enabled to use this site.
							
			.row
				.span3
					%h3 Dashboard
					%ul.nav.nav-pills.nav-stacked
						= nav_link("Home", dashboard_index_path)

						- if can? :manage, :all
							= nav_link("Admin Panel", admin_index_path)

						- if can? :manage, DepartmentBlock
							= nav_link("Manage Blocks", dashboard_department_blocks_path)

				.span9
					= yield
